import styled from 'styled-components';

export const MainWrapper = styled.main`
  position: relative;
`;

export const SwitchViewToggle = styled.div`
  position: fixed;
  z-index: 1050;
  left: 50%;
  bottom: 5rem;
  transform: translateX(-50%);
`;
